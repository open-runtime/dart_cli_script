# Version Bump Rationale

**Decision**: patch

The only change in this release is a chore update to the `runtime_ci_tooling` dev dependency. According to Semantic Versioning and the project's release guidelines, dependency updates and chore/maintenance commits warrant a patch release.

## Key Changes
- Updated `runtime_ci_tooling` dev dependency from `^0.11.0` to `^0.12.0` to fix a `create-release` pipeline failure.

## Breaking Changes
None.

## New Features
None.

## References
- `chore(deps): bump runtime_ci_tooling to ^0.12.0`
